[package]
name = "orchestraitor-cost-ledger"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Per-call cost, subscription utilization, and budget ledger for Orchestraitor"

[lints]
workspace = true

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
orchestraitor-core = { path = "../orchestraitor-core" }
orchestraitor-model = { path = "../orchestraitor-model" }
rusqlite = { workspace = true }
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }

[dev-dependencies]
async-trait = "0.1"
orchestraitor-provider-api = { path = "../orchestraitor-provider-api" }
serde_json = { workspace = true }
tokio = { workspace = true }
