name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 40
    # Disabled until orchestraitor-cli crate exists (tracked in #27).
    # Re-enable with schedule + push triggers when the CLI is implemented.
    if: false
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          - runner: macos-14
            target: aarch64-apple-darwin
    env:
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14  # v4.2.3
      - uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696  # v0.0.10
      - name: Add target
        run: rustup target add ${{ matrix.target }}
      - name: Build release binary
        run: cargo build --release --locked --target ${{ matrix.target }} -p orchestraitor-cli
      - name: Strip binary
        run: strip target/${{ matrix.target }}/release/orc
      - name: Package
        run: |
          mkdir -p dist
          tar -czf dist/orchestraitor-${{ matrix.target }}.tar.gz \
            -C target/${{ matrix.target }}/release orc
          cd dist
          if command -v sha256sum >/dev/null; then
            sha256sum orchestraitor-${{ matrix.target }}.tar.gz > orchestraitor-${{ matrix.target }}.tar.gz.sha256
          else
            shasum -a 256 orchestraitor-${{ matrix.target }}.tar.gz > orchestraitor-${{ matrix.target }}.tar.gz.sha256
          fi
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: orchestraitor-${{ matrix.target }}
          path: dist/orchestraitor-${{ matrix.target }}.tar.gz*

  release:
    name: Publish nightly release
    needs: build
    runs-on: ubuntu-latest
    if: false
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          path: dist
          merge-multiple: true
      - name: Count artifacts
        id: artifacts
        run: |
          mkdir -p dist
          count=$(find dist -type f -name '*.tar.gz*' | wc -l)
          echo "count=$count" >> "$GITHUB_OUTPUT"
          if [ "$count" -eq 0 ]; then
            echo "::notice::No build artifacts available; skipping release creation."
          else
            echo "Publishing $count artifact(s)."
          fi
      - name: Get date and tag
        id: date
        if: steps.artifacts.outputs.count != '0'
        run: |
          echo "date=$(date -u +'%Y-%m-%d')" >> "$GITHUB_OUTPUT"
          echo "tag=nightly-$(date -u +'%Y-%m-%d-%H%M%S')" >> "$GITHUB_OUTPUT"
      - name: Delete old nightly releases
        if: steps.artifacts.outputs.count != '0'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cutoff_ts=$(date -u -d '7 days ago' +'%Y-%m-%d' 2>/dev/null || date -u -v-7d +'%Y-%m-%d' 2>/dev/null)
          gh release list --repo "$GITHUB_REPOSITORY" --limit 100 --json tagName,createdAt --jq '.[] | select(.tagName | startswith("nightly-")) | "\(.tagName) \(.createdAt)"' 2>/dev/null | while read tag created; do
            ts=$(echo "$created" | cut -dT -f1)
            if [[ "$ts" < "$cutoff_ts" ]]; then
              echo "Deleting old release: $tag (created $created)"
              gh release delete "$tag" --repo "$GITHUB_REPOSITORY" --yes --cleanup-tag || true
            fi
          done
      - name: Create release with assets
        if: steps.artifacts.outputs.count != '0'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ steps.date.outputs.tag }}" \
            --repo "$GITHUB_REPOSITORY" \
            --title "Nightly Build (${{ steps.date.outputs.date }})" \
            --notes "Automated nightly build.

          **Build date:** ${{ steps.date.outputs.date }}
          **Commit:** ${{ github.sha }}

          Pre-release binaries. Not for production use." \
            --prerelease \
            dist/*.tar.gz dist/*.sha256
